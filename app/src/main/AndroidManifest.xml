<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.translateapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logonw"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Delete" />
        <activity android:name=".GermanWords" />
        <activity android:name=".RussianWords" />
        <activity android:name=".FrenchWords" />
        <activity android:name=".SpanishWords" />
        <activity android:name=".SavedTranslations" />
        <activity android:name=".Subscribe" />
        <activity android:name=".Translate" />
        <activity android:name=".EditPhrase" />
        <activity android:name=".DisplayPhrase" />
        <activity android:name=".AddPhrase" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>